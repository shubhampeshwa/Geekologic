import { useEffect } from "react"
import { MainLayout } from "../../components"

const Home = ({ navigation }: any) => {

    useEffect(() => {
    }, [])

    return <MainLayout headerTitle={"My Recorder"}>

    </MainLayout>
}

export default Home